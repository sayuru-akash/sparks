/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "127.0.0.1", "sparks-by-codezela.ghost.io", "static.ghost.org", "www.gravatar.com"],
  },
};

module.exports = nextConfig;
